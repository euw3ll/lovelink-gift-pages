// src/components/ui/theme-previews/types.ts

export interface PreviewProps {
  name1: string;
  name2: string;
  uploadedImage: string | null;
  // Podemos adicionar mais campos simples aqui no futuro, se necessário
}
